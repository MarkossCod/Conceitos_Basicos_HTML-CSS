## 🚀 Introdução ao HTML e CSS: Seus Primeiros Passos no Desenvolvimento Web

Olá! Este repositório contém um projeto simples e **interativo** projetado para ajudar você a entender os **conceitos básicos** de **HTML** (HyperText Markup Language) e **CSS** (Cascading Style Sheets).

Se você é novo no desenvolvimento web, este é o lugar perfeito para começar a construir a **estrutura** e estilizar a **aparência** de suas primeiras páginas web!

---

## 🎯 O que você vai aprender

Este projeto demonstra os seguintes conceitos fundamentais:

### 📄 HTML (A Estrutura da Web)

O HTML é como o esqueleto de uma página web. Ele define o **conteúdo** e a **estrutura**.

* **Tags e Elementos:** Como usar tags como `<h1>`, `<p>`, `<img>`, `<a>` para criar cabeçalhos, parágrafos, imagens e links.
    * **Exemplo:** A tag `<p>` cria um novo parágrafo de texto.
* **Estrutura Básica:** Entender a organização de um documento HTML (usando tags como `<!DOCTYPE html>`, `<html>`, `<head>` e `<body>`). 
* **Atributos:** Usar atributos como `href` (para links) e `src` (para imagens) para dar mais informações aos elementos.
    * **Exemplo:** `<a href="https://www.google.com">Ir para Google</a>`

### 🎨 CSS (A Beleza da Web)

O CSS é o responsável por toda a **estilização** e **design** da sua página.

* **Seletores:** Como "selecionar" elementos HTML específicos para aplicar estilos (usando seletores de **elemento**, **classe** e **ID**).
    * **Exemplo:** Selecionar todos os parágrafos com `p { ... }` ou uma classe específica com `.minha-classe { ... }`.
* **Propriedades e Valores:** As regras básicas de estilo (por exemplo, `color: blue;`, `font-size: 16px;`, `background-color: #f0f0f0;`).
* **Modelo de Caixa (Box Model):** Entender como todo elemento HTML é tratado como uma caixa com **conteúdo**, **padding** (preenchimento), **border** (borda) e **margin** (margem). 

[Image of the CSS Box Model]

* **Conexão Externa:** Como vincular um arquivo CSS externo ao seu HTML ( `<link rel="stylesheet" href="style.css">`).

---

## 💡 Como Usar o Projeto (Interativo!)

A melhor forma de aprender é fazendo. Siga estes passos:

### 1. 📂 Clone o Repositório

Abra seu terminal ou prompt de comando e execute:

```bash
git clone [https://cursos.alura.com.br/forum/topico-repositorio-interno-35333](https://cursos.alura.com.br/forum/topico-repositorio-interno-35333)
cd [NOME DO SEU REPOSITÓRIO]
